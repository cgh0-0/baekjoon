/*한 줄 요약
"복잡한 예외(6, 9)는 먼저 하나로 뭉쳐서 숫자로 가공한 뒤, 마지막에 단순 비교(max)로 끝낸다."
1475번 복기: 핵심 3줄 요약
정의: '방 번호' 문제는 각 숫자의 빈도를 체크하되, 6과 9라는 '교환 가능한 예외'를 처리하는 최댓값 찾기 문제다.

원리: 예외(6, 9)를 먼저 하나로 합쳐서 '필요한 세트 수'로 가공(Pre-processing)한 뒤, 전체에서 최댓값을 찾으면 논리가 단순해진다.

실수 포인트: 루프 중간에 실시간으로 max를 수정하려고 하면, 특정 조건(예: 9가 6보다 적을 때)에서 계산이 누락되는 논리적 점프가 발생한다.

📊 시각화, 더 했어야 했나? (패턴 훈련)
"시각화해서 뭐 나온 게 없다"고 느끼셨던 이유는 아마 **'성공하는 케이스'**만 머릿속으로 그리셨기 때문일 거예요. 시각화의 진짜 목적은 **'내 논리가 깨지는 지점'**을 찾는 겁니다.

다음에는 아래 3가지 유형의 테스트 케이스로 표를 그려보세요:

Case 1 (일반): 123 -> 6/9가 없을 때 내 max가 잘 작동하나?

Case 2 (6, 9가 짝수/홀수일 때): 666 vs 6666 -> (n+1)/2 공식이 2세트와 2세트로 잘 나오나?

Case 3 (엣지 케이스): 69696 -> 6과 9가 섞여서 많이 나올 때, 내 코드가 6만 있을 때와 동일하게 3세트를 뱉어내나?

1. [설계] 관심사의 분리 (Data Processing)실시간(Loop 안) 계산 금지: 루프 돌면서 매번 max를 수정하면 예외 처리가 꼬임.3단계 공식: 데이터 수집(Count) → 데이터 가공(6, 9 합치기) → 결과 도출(Max 찾기)2. [수학] 정수 올림 테크닉공식:$$(N + 1) / 2$$용도: 홀수/짝수 구분 없이 "2개당 1세트"가 필요할 때 사용.$N=4$ 일 때: $5 / 2 = 2$$N=5$ 일 때: $6 / 2 = 3$ (자동으로 1세트 추가됨)3. [검증] 시각화의 목적성공하는 케이스가 아니라, **내 논리가 깨질 것 같은 '최악의 케이스(6만 많을 때, 9만 많을 때 등)'**를 표로 그려보는 것이 진짜 GPU 훈련임.*/

#include<iostream>
#include<string>
int main()
{
	std::string input;
	std::cin >> input;
	int arr[10] = {0};
	for (int i = 0; i < input.size(); ++i)
	{
		arr[input[i] - '0'] += 1;
		
	}
	int max = 0;
	int set69 = arr[6] + arr[9];
	arr[6] = (set69 + 1) / 2;
	arr[9] = 0;
	for (int i = 0; i < 10; ++i)
	{
		if(max<arr[i])
		{

		 max = arr[i];
				
		}

			
		
		
		
	}
	std::cout << max;



}

